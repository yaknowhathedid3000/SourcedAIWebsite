// The Payout Room — site configuration.
// This is the only file that needs editing to take the funnel live.
window.PAYOUT_ROOM_CONFIG = {
  // Paste the live checkout link (Skool, Whop, or Stripe Payment Link).
  // While empty, /checkout shows the demo checkpoint page instead.
  checkoutUrl: "",

  // Optional: a YouTube / Vimeo / Loom embed URL. When set, the hero
  // shows the video instead of the key-levels chart.
  videoEmbedUrl: "",

  // Social + community links.
  instagramUrl: "https://www.instagram.com/savvybtrades",
  discordUrl: "",

  // Shown in the nav and footer.
  instagramFollowers: "41K",
};
